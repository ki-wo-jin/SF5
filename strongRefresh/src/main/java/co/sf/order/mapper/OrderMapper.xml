<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.sf.order.mapper.OrderMapper">


	<insert id="addAddress" parameterType="co.sf.order.vo.OrderVO">
		INSERT INTO order_f5 ( order_code, id, recipient, order_date, order_status,
		phone, address, delivery_message, use_point, total_price, email)
		
		VALUES (#{orderCode}, #{id}, #{recipient}, #{order_status}, #{phone},
		#{address},	#{deliveryMessage},	#{usePoint}, #{totalPrice},	#{email})
	</insert>


	<delete id="removeCartItem" parameterType="int">
		DELETE FROM
		cart_f5
		WHERE cart_code = #{cartCode}
	</delete>


</mapper>
